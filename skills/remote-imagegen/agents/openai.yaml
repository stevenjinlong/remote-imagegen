interface:
  display_name: "Remote Imagegen"
  short_description: "Generate detailed raster images through a custom Codex endpoint"
  default_prompt: "Use $remote-imagegen to create a detailed 4K raster image through the OpenAI-compatible base URL configured in the local Codex installation. For technical diagrams, include concrete components, connectors, Chinese labels, color semantics, and an avoid list."

policy:
  allow_implicit_invocation: true
